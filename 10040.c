#include<stdio.h>
#include<string.h>
int in[110];
int dp[32100];
int road[32100];
main()
{
	int N,T;
	int goal;
	scanf("%d",&T);
	while (T--)
	{
		scanf("%d",&N);
		for (i=0;i<N;i++)
		scanf("%d",&in[i]);
		scanf("%d",&goal);		
		int w=in[0];
		for (int j=0;j<4;j++)
		for (int i=1;i<N;i++)
			
	}
}
